-- =====================================================================
-- MIR SMART — 간섭 이슈의 상대측(B) GlobalId 앵커 추가 — S49 (#8/#9).
-- 간섭 → 이슈로 만든 경우, '위치 보기'에서 간섭체크 뷰(A↔B)를 그대로 복원하려면
-- A(global_id, 0026) 외에 **B측 GlobalId** 도 필요하다. 일반 이슈는 비어 있다.
-- 추가형·멱등.
-- =====================================================================

alter table public.issues
  add column if not exists global_id_b text;
