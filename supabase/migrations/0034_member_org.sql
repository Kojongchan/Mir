-- =====================================================================
-- MIR SMART — 구성원·권한 확장: 소속(회사) + 담당업무
--
-- 사용자 요청: 구성원 표에 소속(회사명)과 담당업무를 넣어 실무 조직을 표현한다.
--   • company(소속) — 발주처·감리단·시공사·BIM 협력업체 등. 실제 회사명(쌍용건설(주),
--     KCMC, 국가철도공단 …)을 자유 입력.
--   • duty(담당업무) — 현장 공무팀·현장 공사팀·본사 BIM 담당자·BIM 관리자·BIM 실무자 등.
--
-- 프로젝트 단위 속성이라 project_members 에 둔다(같은 계정도 프로젝트마다 소속/역할이
-- 다를 수 있음). 권한(role) 자체는 0001/0023 그대로. 쓰기(update)는 0023 members_update
-- (프로젝트 관리자 이상)로 이미 커버 → 별도 정책 불필요.
--
-- 추가형 · 멱등 — 0001..0033 무수정.
-- =====================================================================

alter table public.project_members
  add column if not exists company text,  -- 소속(회사/기관)
  add column if not exists duty    text;  -- 담당업무(직무)

notify pgrst, 'reload schema';
